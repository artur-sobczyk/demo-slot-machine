{
  "name": "slot-machine-frontend",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "esbuild src/app.js --bundle --minify --outfile=static/app.bundle.js --format=iife --platform=browser --target=es2020"
  },
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.600.0",
    "@aws-sdk/client-cognito-identity": "^3.600.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.0"
  }
}
